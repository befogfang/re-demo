export interface SiteConfig {
    brandName: string;
    themeColor: string;
    headerBg: string; // Hex color
    heroTitle: string;
    heroSubtitle: string;
    heroImage: string; // URL
    address: string;
    mapUrl: string; // Full Google Maps Link
}
